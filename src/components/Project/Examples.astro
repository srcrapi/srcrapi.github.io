---
interface Props {
    src: string;
    alt: string;
}

const { src, alt } = Astro.props;
---

<div class="flex justify-center my-5">
    <img
        src={src}
        alt={alt}
        class="rounded-lg w-2/3 h-auto max-[800px]:w-[90%]"
    />
</div>
